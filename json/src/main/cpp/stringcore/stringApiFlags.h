#pragma once

#define flagUnicode 0x01
#define flagAnsi 0x02
#define flagLen 0x04
#define flagNoLen 0x08
#define flagHintBufferSize 0x10    // Returns -(cchMinBufferSize) if passed buffer is too small
